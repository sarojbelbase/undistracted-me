/**
 * @deprecated Import from 'src/hooks/useAgeLabel' instead.
 * This re-export exists so existing `from '../../utilities/useAgeLabel'` paths keep working.
 */
export { useAgeLabel } from '../hooks/useAgeLabel';

