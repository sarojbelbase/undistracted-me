/**
 * @deprecated Import from 'src/hooks/useAutoTheme' instead.
 * This re-export exists so existing `from './utilities/useAutoTheme'` paths keep working.
 */
export { useAutoTheme } from '../hooks/useAutoTheme';

